import React, { useCallback } from 'react';
import { SlatePlugins } from '@udecode/slate-plugins'

const RocketSlate = ({id}) => {
  return (
    <SlatePlugins>
    </SlatePlugins>
  );
}

export default RocketSlate;
