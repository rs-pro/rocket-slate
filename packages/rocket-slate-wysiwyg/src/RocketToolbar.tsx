import React from 'react';
import { Toolbar } from 'slate-plugins-next';
import styled from 'styled-components';

const RocketToolbar = styled(Toolbar)`
  padding: 10px;
  border-bottom: 2px solid #ccc;
`;

export { RocketToolbar };
